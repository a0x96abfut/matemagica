# qual o caminho do meio? a.k.a media entre dois valores?>??>?>
meanAa=(a+A)/2

# salvar variaveis de ambiente (runtime) para um MAT-file
save bifut.mat

# limpeza de tela (~~~atencao LINUX)
clc

# remover todas variaveis (~~~atencao usuarios de Kerneis LINUX, comando gatilho, nao confunda com clear screen!)
clear

# veja os conteudos das operacoes, simplismente digite o nome da variavel
laVarivel

# constantes built in
pi
abs (absoluto valor)
eig (eigenvalues)

# controlar precisao de output
format {long/short/...}
